# Parity — `CreatorOnboardingStage` (`resonance_moment`) ⇄ `1473:81571`

## Verdict

> **`app.png` matches `design.png` except:** [shared deltas S1–S6 · composer width and absent
>
> > `+`/mic as on screen 16 · "Yes I’m ready" and Skip drawn under the prompt where the frame draws
> > no action · the quoted line starts flush rather than 5px in].

Deltas: **9 fixed · 8 accepted · 1 open.** Shared deltas S1–S7 and the capture method:
[`../14-creator-opening/parity.md`](../14-creator-opening/parity.md). Composer fixes (5) and
composer deltas: [`../16-creator-offering/parity.md`](../16-creator-offering/parity.md).

## Measurements (after fixes)

| Element       | design (ink)                         | app (ink)         | Δ               |
| ------------- | ------------------------------------ | ----------------- | --------------- |
| prompt lines  | y 109 / 133 / 157                    | y 109 / 133 / 157 | exact           |
| line 3 start  | x=127                                | x=120             | delta 2         |
| line 3 weight | bold (“Yes… this is why I do this”?) | bold              | exact           |
| composer      | y 846–941                            | y 846–941         | exact; width S1 |

## Screen-specific deltas

1. **"Yes I’m ready" and Skip drawn; the frame draws neither.** **Accepted:** named by
   [`design.md`](design.md)'s state contract. The stage is optional, so Skip is offered.
2. **The quoted line starts at x=120, the frame's at x=127.** The frame's text run begins with a
   space before the opening quote; the transcribed copy does not carry it. **Accepted:** a
   transcription artifact, not a layout rule — the other two lines agree.
3. **Composer width / `+` and mic** — as screen 16 deltas 6 (open, part of S1) and 7 (accepted).
