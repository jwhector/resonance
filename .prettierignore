# Dependencies (prettier ignores node_modules by default, listed for clarity)
node_modules/

# Build outputs
.next/
dist/
out/
build/
coverage/

# Turbo cache
.turbo/

# Drizzle-kit generated migration metadata — do not hand-format
packages/*/drizzle/

# SDD scratch — gitignored, not real source
.superpowers/

# Tool-managed knowledge store (mulch generates README/records) — do not hand-format
.mulch/

# The compiled Weave OS corpus — emitted by packages/weave-os/scripts/compile-corpus.ts.
# `corpus:check` compares it byte-for-byte against a fresh compile, so reformatting it
# would make the build report drift that does not exist.
packages/weave-os/src/corpus.generated.ts

# Weave design specs — verbatim Google Docs exports kept as source material for the
# Weave OS corpus (ADR-0020). Reformatting would alter the escaped characters we
# transcribe from, so they are read as-is and never rewritten.
docs/Weave*.md
docs/Emerging Creator Onboarding Interview Flow.md
