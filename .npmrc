# Strict, content-addressed installs keep package boundaries honest:
# a package can only import what it explicitly declares as a dependency.
# This is load-bearing for agent context hygiene (see ADR-0003).
auto-install-peers=true
strict-peer-dependencies=false
# Do NOT hoist everything to the root — phantom deps blur package boundaries.
shamefully-hoist=false
