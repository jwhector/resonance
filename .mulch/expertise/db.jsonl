{"type":"reference","classification":"foundational","recorded_at":"2026-07-07T05:35:35.071Z","evidence":{"file":"docs/adr/0004-postgres-drizzle-pgvector.md","commit":"5801fd7549f090d7b04363383b360f810ac0461a"},"tags":["adr","ratified-decision"],"dir_anchors":["packages/db"],"name":"ratified decisions (ADR index)","description":"Single data layer = Drizzle over Neon Postgres + pgvector; matching via embeddings, dims pinned to Voyage voyage-3.5 = 1024. Open ADR-0004 / ADR-0010 before changing schema strategy or embedding dims.","files":["docs/adr/0004-postgres-drizzle-pgvector.md"],"id":"mx-152a17"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-07-27T00:46:55.266Z","evidence":{"seeds":"resonance-663d","commit":"9fa636a0f24876b14a74287a0c76effc90715740"},"name":"Discovery ANN read: one deep query, wrappers never re-query","description":"packages/db/src/queries/discovery.ts owns the ONLY ANN read over creator profiles. searchCreatorProfiles holds every correctness rule (status='ready', assertEmbeddingDims before SQL, conjunctive jsonb @> tags, similarity floor, similarity DESC + id ASC total order, keyset pagination, per-viewer follow state). findSimilarProfiles is a thin wrapper mapping its rows, NOT a second query — so the ADR-0018 live-smoke gate and product discovery exercise one implementation. Rule for future work: add an argument, never a parallel query. Two structural details worth copying: (a) fetch limit+1 so nextCursor is non-null IFF more rows follow rather than merely 'iff the page was full' — an exactly-full final page is the test that catches the naive version; (b) GROUP BY creator_profiles.id with max(similarity) so one row per creator is structural, not an accident of there being one embedding model, which forces threshold/cursor predicates into HAVING.","files":["packages/db/src/queries/discovery.ts","packages/db/src/queries/profiles.ts"],"id":"mx-c0e190"}
{"type":"failure","classification":"tactical","recorded_at":"2026-07-27T00:47:07.083Z","evidence":{"seeds":"resonance-663d","commit":"9fa636a0f24876b14a74287a0c76effc90715740"},"description":"packages/db/src/queries/profiles.ts findSimilarProfiles filtered only on embeddings.source_type, never on creator_profiles.status, so unpublished draft profiles leaked into every discovery read — a live correctness bug, not a missing feature. It was invisible because a draft looks like any other ranked hit. Related silent-failure class in the same query: pgvector does NOT error on a wrong-width vector, it matches nothing, so a dimension mismatch is indistinguishable from 'no results' (ADR-0010). A third, latent one: the embeddings unique index is (source_type, source_id, model), so a creator embedded under a second model joins to two rows, and two rows sharing a profile id defeat the id pagination tiebreaker — silently skipping or repeating creators across pages.","resolution":"Filter status='ready' inside the query so no read path can forget it (drafts stay visible to direct getCreatorProfileById/ByUserId lookups — the owner must see their own draft). Call core's assertEmbeddingDims() before touching SQL so a width mismatch throws EmbeddingDimensionError. GROUP BY creator_profiles.id with max(similarity) so one row per creator is structural. Each fix is pinned by a test PROVEN to fail against the old query first — that proof is the point, not the test count.","id":"mx-c9ba26"}
{"type":"decision","classification":"tactical","recorded_at":"2026-07-27T00:47:16.660Z","evidence":{"seeds":"resonance-663d","commit":"9fa636a0f24876b14a74287a0c76effc90715740"},"title":"follows edges are user-to-user, and follow mutation stays off DiscoveryPort","rationale":"The edge is (follower_id, following_id) over Better Auth user ids, not user-to-profile, because a creator profile is a REGENERABLE projection of a user (createCreatorProfile upserts on user_id) — hanging follows off the profile uuid would make the graph fragile to regeneration, and following is a member-to-member capability the moment the community slice grows, so this shape needs no migration then. The composite PK is the row's whole identity: it makes followCreator idempotent via onConflictDoNothing (a double-clicked Follow button is a UI accident, not a conflict) and indexes the 'does A follow B' direction for free; follows_following_idx serves the reverse. Self-follows throw ResonanceError('follow_self') rather than hitting a CHECK constraint, because the web layer needs a branchable typed error, not an opaque driver one. Follow MUTATION deliberately does not sit on core's DiscoveryPort — it is not ranking, and putting it there would force every alternative ranker (resonance-7890) to reimplement it.","id":"mx-5ec546"}
{"type":"failure","classification":"tactical","recorded_at":"2026-07-27T04:54:20.011Z","evidence":{"seeds":"resonance-3f15","commit":"d1fefa94416dc7f444117cef21fc97d1d4d91cfe"},"description":"An authenticated /discover search 500s on the dev Neon DB with NeonDbError 42P01 relation \"follows\" does not exist, while the anonymous search is fine — so the bug looks like an auth problem and is not. searchCreatorProfiles only emits its correlated 'exists (select 1 from follows ...)' sub-select when viewerId is non-null; the viewer-null branch emits the literal 'unknown' and never names the table. A committed, merged migration is therefore invisible until someone signs in. Root cause: packages/db/drizzle/0003_green_ultimo.sql shipped in resonance-663d but drizzle.__drizzle_migrations on the dev database had only 3 of 4 rows.","resolution":"Check the DATABASE the code will actually run against, not just the migrations folder: select table_name from information_schema.tables where table_schema='public', and compare drizzle.__drizzle_migrations row count against packages/db/drizzle/*.sql. Apply with 'pnpm --dir packages/db db:migrate' (needs DATABASE_URL in the environment; drizzle.config.ts reads process.env, and apps/web/.env.local is the only place it lives locally). Filed as resonance-7912.","id":"mx-a981bb"}
{"type":"failure","classification":"tactical","recorded_at":"2026-07-28T05:07:56.154Z","evidence":{"seeds":"resonance-e030","commit":"831a8af32c82cc09b014fb71d41ca4b80dc86691"},"description":"Reaching for a DB transaction to make a multi-row or multi-table write atomic in @resonance/db does not work: the two drivers behind 'type Db' support disjoint mechanisms. drizzle's neon-http session throws 'No transactions support in neon-http driver' on db.transaction(), and the PGlite harness used in tests has no db.batch(). So neither db.transaction() nor db.batch() compiles to working code on both, and a helper that uses either passes in tests and fails in production (or vice versa).","resolution":"Make the write ONE Postgres statement — every single statement runs in its own implicit transaction, which is the only atomicity mechanism both drivers share. A batch insert is one multi-row INSERT. A multi-table write puts the extra tables in data-modifying WITH clauses via db.execute(sql`with x as (${insert.getSQL()}) ${mainInsert.getSQL()}`); drizzle insert builders are SQLWrapper so .getSQL() composes them without executing. Postgres runs a WITH clause exactly once and to completion, and child-row foreign keys resolve because RI triggers fire after the statement finishes. Pick the always-non-empty insert as the main statement, since a WITH clause inserting zero rows cannot be written. See packages/db/src/queries/observations.ts.","id":"mx-111142"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-07-28T05:08:07.386Z","evidence":{"seeds":"resonance-e030","commit":"831a8af32c82cc09b014fb71d41ca4b80dc86691"},"name":"Ports that must not default a value take only the deps they need","description":"createObservationAdapter({ db }) enforces core's ObservationPort invariant 'never substitute a current OS release' structurally rather than by discipline: the deps object contains no release, so there is nothing the adapter could fall back to. The column is NOT NULL with no DB default for the same reason, and a small assertAttributable guard rejects a blank string that NOT NULL would accept. Compare createDiscoveryAdapter({ db, embed }), which injects what it does need. The general rule: when a port forbids an adapter from inventing a value, the cheapest enforcement is to give the adapter no source for it — a missing dependency cannot be misused, whereas a present one relies on every future edit remembering the rule.","files":["packages/db/src/adapters/observation-adapter.ts","packages/db/src/schema/weave-observation.ts"],"id":"mx-1f0458"}
{"type":"decision","classification":"tactical","recorded_at":"2026-07-28T05:08:18.297Z","evidence":{"seeds":"resonance-e030","commit":"831a8af32c82cc09b014fb71d41ca4b80dc86691"},"title":"Weave pattern-registry records: jsonb metric maps, enums only where the source enumerates","rationale":"The Pattern Registry's ten evidence counts and nine classification scores are stored as two jsonb columns on weave_patterns, not nineteen integer columns. The metric NAMES belong to the registry rulebook, which ADR-0020 §4 places as corpus YAML in @resonance/weave-os; a column per metric would make the db schema a second copy of that list that can silently diverge, and adding a metric would cost a migration instead of a corpus edit. PatternEvidenceMetricsSchema and PatternClassificationScoresSchema type them at the boundary, matching how creator_profiles types its jsonb offerings. The registry holds tens of rows, so no index is justified on those maps — over-indexing a tiny table is the real mistake. Relatedly: lifecycle status (7) and promotion target (4) are TS unions because the source document enumerates them outright, while promotion_readiness, promotion_decision, discovered_during and current_scope_hypothesis have exactly one attested value each and no declared set, so they are carried as plain text rather than a guessed enum. Architect/Validator fields are absent because those entities are out of scope per ADR-0020.","id":"mx-9e403d"}
