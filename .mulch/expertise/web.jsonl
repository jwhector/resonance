{"type":"reference","classification":"foundational","recorded_at":"2026-07-07T05:35:35.819Z","evidence":{"file":"docs/adr/0002-nextjs-app-router-web-only.md","commit":"5801fd7549f090d7b04363383b360f810ac0461a"},"tags":["adr","ratified-decision"],"dir_anchors":["apps/web"],"name":"ratified decisions (ADR index)","description":"apps/web is a thin Next.js App Router shell — web-only, logic stays extraction-ready in packages; RSC + Server Actions, TanStack Query where interactive (ADR-0002, ADR-0008).","files":["docs/adr/0002-nextjs-app-router-web-only.md"],"id":"mx-152a17"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-07-08T00:59:55.411Z","evidence":{"commit":"2d90880","seeds":"resonance-7c37"},"name":"onboarding-wiring","description":"apps/web wires Creator Onboarding as a thin shell: browser Better Auth client (magicLink+emailOTP) for the cookie sign-in flow; useChat(DefaultChatTransport) maps UIMessage<->InterviewMessage via prepareSendMessagesRequest so the interview route Zod-validates one shape; profile-gen runs via runAgentStructured with a RESONANCE_FAKES-only injected canned tool-calling model (ai/test), dynamically imported to stay out of prod; commit redirects to the returned profileId because getCreatorProfileById is a profile-PK lookup with no by-user query exposed.","files":[".seeds/issues.jsonl"],"id":"mx-451cad"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-07-08T07:48:56.931Z","evidence":{"seeds":"resonance-dabd","commit":"bb97575f95e5049523704e45c3472a76e4dbacb1"},"name":"web-live-by-default-di","description":"apps/web is live-by-default (ADR-0018): Server Actions call the live @resonance/ai seam (runAgentStructured with no injected model -> resolveModel; commit ctx passes resolveEmbedder()). No RESONANCE_FAKES runtime branch and no dynamic import of ai/test in shipped code. Tests inject fakes ONLY via DI: actions.test.ts mocks the @resonance/ai public entrypoint (getSession/runAgentStructured/commitCreatorProfile/resolveEmbedder), so unit tests stay green with zero credentials without ever hitting a live path or the removed env flag. The runner's RunInput.model DI seam stays open but is taken by tests only. The E2E OTP harness (app/api/test/last-otp + e2e/*) is a SEPARATE human-gated seed (resonance-a4a4) and still uses RESONANCE_FAKES on purpose.","files":["apps/web/app/onboarding/creator/actions.ts","apps/web/app/onboarding/creator/actions.test.ts","apps/web/app/api/onboarding/interview/route.ts"],"id":"mx-76e0ed"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-07-08T07:49:09.578Z","evidence":{"seeds":"resonance-6bf1","commit":"bb97575f95e5049523704e45c3472a76e4dbacb1"},"name":"usechat-onerror-surface","description":"Surface live streaming failures in the interview UI (AI SDK v6 / @ai-sdk/react). useChat's onError callback sets a streamError state; the client renders an inline, token-only error card (border-danger + text-danger on bg-surface, role=alert) with a 'Try again' Button (variant=outline size=sm) that calls useChat's regenerate() to re-run the last turn and clears the error. A fresh user turn also clears streamError before the new stream starts. Without this, a resolveModel throw or Gateway failure vanishes and reads as a silent no-response. RTL test pattern: since useChat is mocked, capture the options via vi.mocked(useChat).mock.calls.at(-1)?.[0], narrow-cast to read onError (it lives on the ChatInit arm of the UseChatOptions union, so direct property access fails typecheck), fire it inside act(), then assert the alert renders and retry calls regenerate + clears the surface.","files":["apps/web/app/onboarding/creator/interview-client.tsx","apps/web/app/onboarding/creator/interview-client.test.tsx"],"id":"mx-caf8e4"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-07-08T21:40:13.173Z","evidence":{"seeds":"resonance-dabd","commit":"95f6044aafa1abfc19d072754c3194698495ab40"},"name":"e2e-harness-dynamic-import","description":"E2E harness must DYNAMIC-import fakes (not static) so they stay out of the prod bundle; centralize the E2E_HARNESS selection in intent-named accessors.","files":[".seeds/issues.jsonl","apps/web/app/api/auth/[...all]/route.ts","apps/web/app/api/onboarding/interview/route.ts","apps/web/app/api/test/last-otp/route.ts","apps/web/app/onboarding/creator/actions.ts","apps/web/lib/auth.ts","apps/web/lib/e2e-harness.ts","packages/ai/src/gateway.ts","packages/ai/src/runner.ts","packages/ai/src/testing/index.ts","scripts/verify-live.mjs"],"id":"mx-4cc6cf"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-07-27T02:14:42.863Z","evidence":{"seeds":"resonance-c7db","commit":"55c923f438c35b8a890c2a57172973bbee666226"},"name":"session-optional Server Action: viewer resolved server-side, never a query field","description":"apps/web/app/(app)/discover/actions.ts. A Server Action that acts on behalf of a user takes TWO things that must not be merged: the Zod-parsed client payload, and the viewer resolved from getWebSession. `searchCreators(input)` parses `input` with CreatorDiscoveryQuerySchema and separately derives `viewer`, then calls `port.searchCreators(query, viewer)` — the DiscoveryPort signature keeps them apart deliberately, so there is NO identity field on the query for a caller to spoof through the search box. Zod's strip-unknown-keys behaviour does the rest: a payload carrying `userId`/`viewer` loses it at the parse line (asserted by a test). Session-optional means viewer===null is a first-class case (every row's followState becomes 'unknown', DiscoveryPort invariant 5), not an auth failure — the route does not redirect. Contrast the follow mutations in the same file, where auth IS required: they return a discriminated { ok:false, reason:'unauthenticated'|'follow_self' } instead of throwing, because the caller is a button inside a results list and a redirect would cost the page of results; follow_self is translated from @resonance/db's typed ResonanceError, and every other error propagates untouched.","files":["apps/web/app/(app)/discover/actions.ts","apps/web/app/(app)/discover/contracts.ts","apps/web/app/(app)/discover/actions.test.ts"],"id":"mx-90b1f1"}
{"type":"failure","classification":"tactical","recorded_at":"2026-07-27T02:14:56.302Z","evidence":{"seeds":"resonance-c7db","commit":"55c923f438c35b8a890c2a57172973bbee666226"},"description":"A controlled input seeded from a prop with React.useState(prop) goes STALE under a Next.js App Router soft navigation. /discover keeps search + tab state in the URL, and the search field was initialised as useState(query). On a searchParams-only navigation — browser back/forward, or opening a shared /discover?q=... link while already mounted — the App Router re-renders the client component WITHOUT remounting it, so the field kept its old value while the URL and the results moved on. Worse than a cosmetic mismatch: the tab handler calls navigate(nextKind, text), so the next tab click navigated to the STALE ?q=. Caught by the no-mistakes review gate, not by any of the 11 existing RTL tests, because every one of them rendered fresh instead of rerendering with changed props.","resolution":"Use React's documented 'adjusting state when props change' pattern — hold the last-seen prop in state and reset the draft DURING render when it differs: const [syncedQuery,setSyncedQuery]=useState(query); if (query!==syncedQuery){setSyncedQuery(query);setText(query);}. Do NOT use useEffect (renders one frame of stale text first) and do NOT key/remount the component from the page (that also discards in-flight follow state). The guard matters: it resyncs only when the PROP changes, so a query the member is midway through typing on the same URL survives. Regression test = RTL rerender() with a changed prop asserting both the field value AND that a subsequent tab click navigates with the new query.","id":"mx-0916d6"}
{"type":"convention","classification":"tactical","recorded_at":"2026-07-27T02:15:11.536Z","evidence":{"seeds":"resonance-c7db","commit":"55c923f438c35b8a890c2a57172973bbee666226"},"content":"Shared app chrome belongs to a route GROUP layout, not to a component. apps/web/app/(app)/layout.tsx renders @resonance/ui's 80px AppNav once for /onboarding/creator, /creator/[id] and /discover; before this it was composed inside interview-client.tsx, so /creator/[id] had no rail and every new in-app route would have re-composed it. A (parenthesised) group adds NO URL segment — verify with next build's route table after moving files. The (onboarding) group stays outside it: signup/verify/start are cardless-on-white with no rail. When you move a screen into the group, reproduce its box model exactly (the layout is 'flex h-screen w-full overflow-hidden' + AppNav + 'flex min-w-0 flex-1 flex-col overflow-hidden', and each route owns its own scrolling) — the interview screens have captured app.png parity artifacts that a nesting change would invalidate.","id":"mx-265b57"}
